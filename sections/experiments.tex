\section{Experiments}
\label{sec:experiments}

We evaluate AdaSolver on five standard PDE benchmarks and three industrial design prediction tasks, covering both surface and volumetric fields on three-dimensional geometries. We further assess its efficiency in two CFD-based geometry optimization tasks involving two-dimensional airfoils and three-dimensional wings.

\subsection{Benchmarks}
\label{sec:experiment_benchmarks}

Our evaluation spans canonical PDE problems and representative industrial applications. The standard PDE suite comprises Elasticity, Plasticity, Airfoil, Pipe~\citep{li2023geofno} and Darcy flow~\citep{li2021fno}. These public datasets are widely used to benchmark neural operators~\citep{hao2023gnot,wu2024transolver}.

Governing equations, data splits, and input--output mappings are detailed in Appendix~\ref{app:implementation_details}.

For industrial applications, we consider ShapeNet-Car~\citep{umetani2018learning} and DrivAerML~\citep{ashton2024drivaerml} for automotive aerodynamics, and SuperWing~\citep{yang2025superwing} for three-dimensional wing design. In addition to physical-field prediction errors, we evaluate the accuracy of design-relevant aerodynamic quantities, including the drag coefficient $C_D$, lift coefficient $C_L$, and drag-to-lift ratio $C_D/C_L$. We also use the learned PDE surrogates to guide shape optimization of a two-dimensional airfoil and a three-dimensional wing, assessing computational efficiency and aerodynamic prediction accuracy during optimization.

\subsection{Implementation and Evaluation}
\label{sec:experiment_implementation}

As described in \eqref{eq:transolver_physics_state} and~\eqref{eq:transolver_state_attention}, Transolver approximates a continuous integral operator through point aggregation and attention over latent physical states. With the number of physical states fixed, varying the spatial sampling budget changes the number of point samples used to estimate the aggregation integrals. For Airfoil, Pipe, Elasticity, and Plasticity, we adopt geometry-amortized training from Transolver-3~\citep{zhou2026transolver3}. Given a full mesh of $N$ points, we draw a point count $N_t$ uniformly from $\{\operatorname{round}(0.4N),\ldots,N\}$ for each minibatch, then sample $N_t$ mesh points uniformly without replacement. Darcy uses the existing backbone trained on full meshes. The trained backbone is frozen when evaluating different sampling policies and inference budgets.

For a consistent comparison, we treat all input discretizations as unstructured meshes and evaluate sampling budgets of $37.5\%$, $50\%$, and $75\%$ of the full-mesh point count. Uniform subsampling of mesh points serves as the common baseline. We report relative $L_2$ errors for physical-field predictions and summarize performance across budgets using the relative area under the error--budget curve (rAUEC). Let $b_k$ denote the retained fraction of mesh points, with $(b_1,b_2,b_3)=(0.375,0.50,0.75)$, and let $E_{\mathrm{method}}(b)$ and $E_{\mathrm{uniform}}(b)$ denote mean test errors for a given sampling seed. Using trapezoidal integration, we compute
\begin{equation}
    \mathrm{rAUEC}
    :=
    \frac{\sum_{k=1}^{2}(b_{k+1}-b_k)
          \bigl[E_{\mathrm{method}}(b_k)+E_{\mathrm{method}}(b_{k+1})\bigr]}
         {\sum_{k=1}^{2}(b_{k+1}-b_k)
          \bigl[E_{\mathrm{uniform}}(b_k)+E_{\mathrm{uniform}}(b_{k+1})\bigr]}.
    \label{eq:rauec}
\end{equation}
Lower rAUEC is better, with uniform sampling normalized to $1$. We report mean rAUEC and its sample standard deviation over three independent sampling runs with matched Uniform baselines. We also report end-to-end runtime for geometry optimization.

Airfoil uses the same 112 cases with reference $C_L>0.01$ for field and $C_D/C_L$ evaluation, with metric-specific sampling policies and absolute error for $C_D/C_L$. Elasticity uses 200 independent holdout cases.

For industrial benchmarks, ShapeNet-Car combines full-cloud velocity relative $L_2$ with half the surface-pressure relative $L_2$ error. DrivAerML uses surface-pressure relative $L_2$, and SuperWing uses joint relative $L_2$ on the dataset-scaled pressure and skin-friction channels. The aerodynamic metrics are relative errors in $C_D$ for the car datasets and $C_D/C_L$ for SuperWing, with sampling policies selected separately for each metric. ShapeNet-Car and DrivAerML use 100 and 50 test geometries, respectively. SuperWing uses 2,871 cases for field evaluation and a fixed 2,563-case subset for $C_D/C_L$, selected by a calibration-derived lift-magnitude cutoff of approximately $0.071$.

\subsection{Main Results}
\label{sec:main_results}

\paragraph{Standard Benchmarks.}
Table~\ref{tab:standard_benchmarks} compares physics-informed density refinement (PI), coarse-prediction-guided density refinement (CPG), and cache-aware learned refinement (CA) with uniform subsampling across five standard PDE benchmarks. Overall, adaptive sampling reduces rAUEC, showing that point density is a design variable for improving neural operator predictions under a fixed spatial budget. CPG generally improves upon PI, indicating that features of the predicted solution can guide point allocation more effectively than geometry and prescribed heuristics alone. CA achieves performance close to CPG on several benchmarks, supporting the learnability of the field-dependent signals used for density refinement. The differences are particularly clear on Darcy: PI provides little improvement over uniform sampling, whereas CPG and CA achieve similar, substantially lower rAUEC. Thus, even on a regular Cartesian grid, solution-informed density allocation can improve prediction accuracy. These results suggest that the benefit of adaptive sampling depends on how well its allocation signal reflects the solution field, beyond the geometric complexity of the domain.

\input{tables/standard_benchmarks.tex}

\paragraph{Industrial Applications.}
Wind-tunnel testing informs vehicle and aircraft design by assessing aerodynamic performance through quantities such as drag and lift coefficients. Table~\ref{tab:industrial_benchmarks} reports both full-field prediction errors and errors in aerodynamic quantities on ShapeNet-Car, DrivAerML, and SuperWing. The results show that all three methods substantially improve neural operator accuracy across sampling budgets. For aerodynamic quantities, concentrating samples in influential regions can match or even surpass full-mesh accuracy with only $37.5\%$ of the points. This agrees with the physical intuition that some flow regions contribute more strongly to aerodynamic forces than others. Accurate predictions with fewer points can accelerate geometry optimization.

\input{tables/industrial_benchmarks.tex}

\subsection{Inverse Design}
\label{sec:inverse_design}

We use the trained AdaSolver surrogate with the sampling policies tailored to $C_D/C_L$ described above to optimize airfoil and wing geometries. The design objective is to minimize the drag-to-lift ratio.

\paragraph{2D Airfoil.}
We adopt the airfoil design case from Geo-FNO~\citep{li2023geofno}. Figure~3 compares optimization using AdaSolver with CA sampling against optimization using the same surrogate on the full mesh. AdaSolver substantially accelerates the optimization process while maintaining comparable final aerodynamic performance, as verified by CFD, and reaches better designs within the same time budget. The speedup comes from reducing the cost of repeated objective and gradient evaluations through adaptive point allocation.
