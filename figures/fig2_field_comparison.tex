\begin{figure}[t]
    \centering
    \includegraphics[width=\linewidth]{figures/fig2_field_comparison.pdf}
    \caption{\textbf{Adaptive sampling and field prediction.}
    Four test cases at a $37.5\%$ point budget. Upper rows show samples overlaid on
    refinement features; lower rows show signed prediction errors relative to ground
    truth. Each case uses shared color scales across methods. Annotations report
    relative $L_2$ field errors.}
    \label{fig:field_comparison}
\end{figure}
